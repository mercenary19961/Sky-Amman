# Override the user's shell-level NODE_ENV=production so that local
# `npm install` always pulls devDependencies (vite, tailwindcss, plugins, etc.).
# Without this, every fresh install would silently drop the build toolchain.
production=false
